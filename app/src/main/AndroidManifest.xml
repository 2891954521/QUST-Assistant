<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.university.assistant"
    android:versionCode="12"
    android:versionName="1.8.4">

    <uses-permission android:name="android.permission.INTERNET" />

    <uses-permission android:name="android.permission.WAKE_LOCK" />

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES"/>

    <uses-permission android:name="android.permission.CAMERA" />

    <application
        android:name=".App"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:usesCleartextTraffic="true"
        android:networkSecurityConfig="@xml/network_security_config"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".ui.MainActivity"
            android:label="@string/app_name"
            android:windowSoftInputMode="adjustNothing">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity android:name=".ui.CameraActivity"/>

        <activity android:name=".ui.PictureActivity"/>

        <activity android:name=".ui.note.NoteEditActivity"/>

        <activity android:name=".ui.NavigationActivity"/>

        <activity android:name=".ui.UpdateActivity"/>

        <activity android:name=".ui.LogActivity"/>

        <activity android:name=".ui.SettingActivity"/>

        <activity android:name=".ui.school.LoginActivity"/>

        <activity android:name=".ui.school.GetLessonTableActivity"/>

        <activity android:name=".ui.school.GetExamActivity"/>

        <activity android:name=".ui.school.GetMarkActivity"/>

        <activity android:name=".ui.school.GetAcademicActivity"/>

        <activity android:name=".ui.school.AutoEvaluationActivity"/>

        <activity android:name=".ui.accounts.AccountsActivity"/>

        <activity android:name=".ui.accounts.RecordActivity"/>

        <activity android:name=".ui.third.DrinkActivity"/>

        <activity android:name=".ui.third.CpDailyActivity"/>

        <activity android:name=".ui.third.fake.LeaveListActivity"/>

        <activity android:name=".ui.third.fake.LeaveInfoActivity"/>

        <activity android:name=".ui.third.fake.NewLeaveActivity"/>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="com.university.assistant"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/camera" />
        </provider>

        <!-- 桌面小部件 -->
        <receiver
            android:name=".receiver.LessonTable"
            android:label="课表">

            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>

            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/lesson" />
        </receiver>

    </application>

</manifest>